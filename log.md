# Loiter — Combined Site Log

Append-only. Engine work on the aggregator only; city content is logged in each
subproject's own `wiki/log.md`.

## [2026-08-11] init | Схема объединённого сайта

Прочитаны `CLAUDE.md` и логи всех шести субпроектов и весь код пяти городских
движков. Установлено главное: **движки практически идентичны**. `wiki.ts`,
`routes.ts`, `templates.ts` и все четыре CSS-файла совпадают побайтово (с точностью
до переводов строк); остальные файлы расходятся на 2–8 строк. Полный перечень
расхождений:

- `Loiter: {Город}` — в `page-map.ts`, `page-detail.ts`, `shared.ts`, `notes.ts`;
- порт 3001–3005 — в `server.ts`;
- центр карты, zoom и `STATE_KEY` — две строки в `map.js`;
- таймзона и метка района письма заметки — в `notes.ts` (у Берлина «Округ», у
  остальных «Район»);
- домен `thermal` — только у Будапешта;
- вступление и список источников — в `page-about.ts` (каркас страницы одинаков,
  различаются только проза и порядок `src('domain')`);
- `trust proxy` есть только у Берлина — нужен всем за прокси.

Отсюда решение: объединение — не рефакторинг, а вынос ~15 литералов в реестр
городов. Написан корневой `CLAUDE.md` со схемой: единый движок, реестр
`web/src/cities.ts`, URL с city-namespace, субмодули как источник контента.

Зафиксированы четыре решения: лендинг — страница выбора города; контент — git-субмодули
с `shallow = true`; строго один город за раз (без общей карты и кросс-городского
поиска); картинки остаются на месте, вес гасится `--depth 1`.

Отмечено на будущее: ассеты закоммичены в репозитории городов, `.git` Берлина
197 МБ, всего ~420 МБ рабочих копий — главное ограничение деплоя, не разметка.

## [2026-08-11] feat | Фаза 1: скелет, реестр городов, общие константы

Создан `web/` с конфигами по образцу `wiki_dresden` (порт 4848, чтобы не конфликтовать
с 3001–3005 автономных сайтов). CSS перенесён без единой правки — проверено, что все
четыре файла совпадают с версиями Берлина и Дрездена.

- `src/cities.ts` — реестр пяти городов. Пути `contentRoot`/`assetsRoot` **выводятся**
  из поля `dir`, а не дублируются: раскладка семейства (`{dir}/wiki`,
  `{dir}/web/assets`) — фиксированная конвенция. Реестр проверяет свои инварианты при
  импорте: форма слага, отсутствие дублей и коллизий с зарезервированными сегментами
  маршрутизатора.
- `src/about/{slug}.ts` — по файлу на город: перечисление тем вступления и
  упорядоченный список источников с RU/EN-описаниями. Вынесено из реестра, потому что
  у Братиславы 48 строк.
- `src/constants.ts` — общий: `MARKER_COLOR`, `DOMAIN_COLOR`, `LEGEND_TYPES` (включая
  `thermal`) и `UI_STRINGS` в единственном экземпляре. Легенда города собирается
  `legendFor(city)` — фильтр по `city.domains` вместо закомментированной строки в
  каждом городе, как было раньше.
- `src/routes.ts` — URL-билдеры с city-namespace. Namespace обязателен: слаги
  пересекаются между городами (проверено в фазе 2 — см. ниже).
- `src/lang.ts` — типы `Lang`/`Localized`, чтобы реестр и данные about-страниц не
  импортировали друг друга по кругу.

Слаг и папка разведены как отдельные поля: Вроцлав отдаётся на `/wroclaw`, а папка и
репозиторий сохраняют существующее написание `wroclav`.

Замечено при переносе: в `wiki_bratislava/web/src/page-about.ts` есть описание для
`divadelneprechadzky.theatre.sk`, которого нет в списке вывода, — то есть оно никогда
не рендерилось. В реестр не добавлено, чтобы не менять вывод молча.

## [2026-08-11] feat | Фаза 2: многокорневой загрузчик контента

- `src/wiki.ts` — `loadWikiPages(city)` с корнем из реестра. Кэш и `fs.watch`
  раздельные по городам: правка в одном городе не стоит перезагрузки остальных
  четырёх. Параллельные первые запросы разделяют один проход по дереву
  (`inFlight`), иначе пять одновременных обходов Берлина на холодном старте.
- Отсутствующий корень контента больше не валит приложение: если субмодуль не
  выкачан, город отдаётся пустым с предупреждением в лог, остальные работают. Это
  прямое следствие выбора субмодулей — `git clone` без `--recurse-submodules`
  оставляет пустые папки.
- `WikiPage` получил поле `city`. И `filePath` теперь не уезжает клиенту:
  автономные движки делали `JSON.stringify(pages)` целиком, включая абсолютные
  пути с диска сервера. Добавлена проекция `toClientPages()`, её и отдаём в
  `window.DATA`.
- Битые `coords` (не пара конечных чисел) отбрасываются с предупреждением вместо
  того чтобы уехать клиенту и дать неразмещаемый маркер.
- `src/content-check.ts` + `npm run check` — прогон загрузчика по реальным данным.

Результат первого прогона: **935 страниц по пяти городам**, все корни и картинки
карточек на месте, дублей слагов внутри городов нет.

Коллизии слагов между городами подтверждены на реальных данных: `hauptbahnhof` и
`kunstgewerbemuseum` есть и в Берлине, и в Дрездене. Это и есть обоснование
city-namespace в URL. (Ранее в качестве примера был указан `Nikolaikirche` — неверно:
в Берлине страницы называются `Museum-Nikolaikirche` и `Nikolaikirche-Spandau`,
пересечения не дают. Примеры в `CLAUDE.md` и `routes.ts` заменены на фактические.)

Загрузчик даёт 514 страниц по Берлину против 517 файлов на диске — разница в
`index.md`, `log.md` и `overview.md` в корне `wiki/`: это не страницы-сущности, они
не лежат ни в одной из читаемых папок и в список не попадают. Так и задумано.

## [2026-08-12] feat | Фаза 3: сервер, шаблоны, переключатель городов, лендинг

Сайт запускается: `npm start` → http://localhost:4848. Фаза 4 (заметки) закрыта заодно —
`page-detail.ts` импортирует из `notes.ts` четыре вещи, писать страницу объекта дважды
не было смысла.

- `scripts/map.js` — **ровно четыре правки** к дрезденской версии, проверено построчным
  сравнением: `city` в деструктуризации `window.DATA`, `cityBase` вместо `wikiBase`/
  `mapBase`, `setView(city.center, city.zoom)`, `STATE_KEY = city.stateKey`. Городских
  литералов в файле не осталось; остальные 24 КБ логики карты не тронуты.
- `server.ts` — маршруты с city-namespace, static по городу из реестра, `trust proxy`
  (в автономных сайтах он был только у Берлина). Переписывание тела страницы: `[[слаг]]`
  → `/{город}/{слаг}` и `/assets/x.jpg` → `/{город}/assets/x.jpg`, только точный
  корневой префикс — абсолютные URL на другие хосты не задеваются.
- `notes.ts` — `city` едет в теле формы; из реестра берутся префикс темы, таймзона и
  метка района. В письмо добавилась строка «Город».
- `page-cities.ts` + `styles/cities.css` — лендинг. Картинки карточек берутся из ассетов
  субпроектов, ничего не скачивалось и не дублировалось.

Дымовой тест: все маршруты 200, `/nosuchcity` и `/dresden/Brandenburger-Tor` → 404
(города изолированы), `/berlin/Hauptbahnhof` и `/dresden/Hauptbahnhof` отдают разные
страницы, `thermal` в легенде только у Будапешта, центр карты и `stateKey` у каждого
свои, в payload карты ноль вхождений `filePath` и абсолютных путей.

Найдена и исправлена ошибка: счётчик на карточках выдавал «121 страниц» и «23 страниц».
Русское числительное так не согласуется, и правило не сводится к `n === 1` — 121 требует
«страница», 23 «страницы», а 11–14 «страниц» независимо от последней цифры. Написан
`src/plural.ts`, прогнан на 19 граничных числах.

## [2026-08-12] convention | Единая шапка, `/about` отдельно от источников

По замечанию: шапка должна быть одна на весь сайт, а значит «О проекте» нужен и на
странице выбора городов.

- `renderHeader(city | undefined, …)` — один рендер на все страницы, включая лендинг.
  `renderCitiesHeader` удалён. Различаются только цели ссылок, не структура.
- Появился **`/about`** — общая информация о проекте без источников, со списком городов
  на месте блока источников. `/{city}/about` — тот же общий текст плюс источники этого
  города. Общий текст живёт в одном месте кода и рендерится в шести контекстах: читатель,
  попавший на страницу города, не должен уходить куда-то ещё, чтобы понять, что за проект.
- Логотип `Loiter` теперь ведёт на лендинг со всех страниц. Из-за этого пропала бы
  ссылка на карту города со страницы `/{city}/about`, поэтому выпадающий список
  показывает **все** города, включая текущий: текущий помечен и ведёт на свою карту.

Сверены списки источников с оригиналами: берлин 17, будапешт 19, дрезден 17, вроцлав 10,
братислава 48 — совпадают везде. (Ранее я написал «47» у Братиславы — неверно, исправлено
в `CLAUDE.md` и выше.) Заодно подтвердился «сирота»: описаний 49 при 48 в списке.

## [2026-08-12] init | Репозиторий и субмодули

`git init`, ветка `main`, origin `git@github-personal:Bonzatina/loiter.git`, первый пуш
(`3842cff`, 45 файлов).

Про аккаунт: в задании был URL `git@github.com:...`, поставлен алиас `github-personal`.
Это тот же репозиторий, но `git@github.com` взял бы ключ по умолчанию `~/.ssh/id_ed25519`
— **рабочий** аккаунт. Личный ключ подключается только через алиас, поэтому его и
используют все субпроекты. Идентичность коммитов задана локально для этого репозитория
(`kulikov <martymckul@gmail.com>`), потому что глобальный конфиг корпоративный
(`sergei.kulikov@devexpress.com`); проверено через `git var GIT_AUTHOR_IDENT`.

Пять городов добавлены субмодулями на существующие пути — git подхватил их на месте
(«Adding existing repo at …»), без клонирования и без сети. В `.gitmodules` дописаны
`branch = main` и `shallow = true`. Указатели совпадают с текущими HEAD субпроектов.
После всей операции все пять `git status` clean: указатель живёт только в родительском
репозитории.

В коммит не попало ни байта ассетов, контента `wiki/`, `raw/` и `node_modules`.
`wiki_rural_travel` внесён в `.gitignore` с объяснением, чтобы git не считал его
неотслеживаемым вложенным репозиторием.

Замечено в субпроектах (не менял): у `wiki_dresden` и `wiki_wroclav` нет локального
`user.email`, поэтому последний коммит Дрездена подписан корпоративным адресом.

Открытый вопрос к деплою: алиас `github-personal` есть только на этой машине, на
сборочном хосте `git submodule update --init` по нему не пройдёт. Ограничение описано
в самом `.gitmodules`.

## [2026-08-12] feat | Геолокация на странице выбора городов

Проект нужен человеку, который прямо сейчас стоит на улице, поэтому лендинг предлагает
пропустить выбор: «⊕ Найти меня» определяет позицию, считает ближайший город и открывает
его карту, центрированную на читателе, с маркером его положения. Переиспользован
`setUserPosition` из контрола «⊕ Где я?» — реализация маркера пользователя одна.

Три решения, каждое обосновано:

- **Позиция передаётся через `sessionStorage`, не в URL.** Вариант `?at=52.52,13.40` был
  бы проще, но точное местоположение осело бы в логах сервера, в заголовке `Referer` и в
  хите аналитики. `scripts/cities.js` пишет ключ `loiter_locate`, `scripts/map.js` читает
  его один раз и удаляет, игнорируя записи старше двух минут и чужого города.
- **Порог `NEAR_KM = 30`, а не 60, как было сначала.** Пробник показал, что с 60 человека
  из Вены (55 км) отправляло на карту Братиславы, где вокруг него нет ни одного объекта.
  30 км покрывает внешние округа Берлина (~23 км от центра) и не дотягивается до Вены и
  Праги (119 км). Дальше порога — честный текст «ближайший — X, около N км» со ссылкой,
  без редиректа. Формулировка «вы рядом с X», не «в X»: Потсдам в 27 км проходит порог.
- **Кнопка отдаётся с `hidden`** и раскрывается скриптом только при наличии
  `navigator.geolocation` — без JS мёртвого контрола не появится, как и у формы заметок.

Логика проверена прогоном самого `scripts/cities.js` в node через `vm` с заглушками
браузера на 16 реальных точках (не копии формулы, а того кода, что уедет в браузер):
промахов по выбору города нет. Две ошибки в том прогоне были в тестовых данных, не в
коде — неверная долгота Чунова и ожидание, что Париж ближе к Берлину (до Дрездена 849 км,
до Берлина ~880).

Дополнительно: при приёме передачи сбрасывается восстановленное из прошлого визита
выделение области — иначе оно скрыло бы ровно то, что вокруг читателя.

## [2026-08-12] fix | Личная идентичность во всех репозиториях, переписаны Дрезден и Вроцлав

`user.name = bonzatina`, `user.email = martymckul@gmail.com` заданы в **локальном**
конфиге всех семи репозиториев семьи (проверено через `git var GIT_AUTHOR_IDENT`).
Глобальный конфиг оставлен корпоративным: его подхватывают рабочие репозитории, и
сделав его личным, вы начнёте подписывать рабочие коммиты личным адресом. Соглашение
записано в `CLAUDE.md` — репозиторий без локального переопределения молча коммитит под
рабочим адресом, именно так это и попало в Дрезден и Вроцлав.

Переписана история двух репозиториев (`filter-branch --env-filter`, `filter-repo`
в системе нет), автор и коммиттер:

| | было | стало | коммитов |
|---|---|---|---|
| `wiki_dresden` | `7dbe8d0` | `b414e6f` | 14 |
| `wiki_wroclav` | `260e151` | `db09403` | 4 |
| `loiter` (корень) | `00a1f68` | `78cc020` | 2 |

Содержимое не изменилось: хеши деревьев до и после совпадают, `git diff` против
`refs/original` пуст. В корне — единственное содержательное изменение: указатели
субмодулей Дрездена и Вроцлава, переписанные тем же проходом через `--index-filter`.
Иначе они ссылались бы на коммиты, которых больше нет нигде. Локальные бэкапы старой
истории лежат в `refs/original/refs/heads/main` в каждом репозитории.

**Про пуш Дрездена — на будущее.** Прямой `push --force` оборвался на 45 МБ из 77.
Причина не в сети: у переписанной истории нет ни одного общего коммита с remote, поэтому
обмен «что у вас есть» не исключает ничего — `git rev-list --objects new --not old` даёт
все 953 объекта, хотя деревья и блобы на remote те же самые. Git заливает полный снимок.

Обход: залить историю по коммитам во временную ветку `_rewrite`, тогда каждый шаг —
обычный инкремент к предыдущему (самый крупный кусок 291 объект из 953), после чего
`main` переводится одним пустым пушем и временная ветка удаляется. `main` при этом ни
секунды не находится в промежуточном состоянии. Все 14 шагов прошли с первой попытки.

Решено оставить `kulikov` в истории Берлина (22 коммита), Братиславы (25), Будапешта (4)
и rural_travel (74): адрес там и так личный, переписывать незачем. Требование — чтобы в
корневом репозитории его не было и чтобы все дальнейшие коммиты шли как `bonzatina`.

Корпоративного адреса не осталось нигде: 0 вхождений в `%ae`/`%ce` во всех семи
репозиториях.

**Локального конфига для «всех дальнейших коммитов» недостаточно**: он лежит в
`.git/config`, не коммитится и не переживает клонирование — свежий клон снова унаследует
корпоративный глобальный конфиг, то есть ту же ловушку. Поэтому в `~/.gitconfig` добавлено
условное включение:

```
[includeIf "gitdir/i:C:/Users/sergei.kulikov/projects/Loiter/"]
	path = ~/.gitconfig-loiter
```

Проверено: свежий репозиторий, созданный внутри `Loiter` без всякого локального
переопределения, даёт `bonzatina <martymckul@gmail.com>` из `~/.gitconfig-loiter`;
`C:\work\dp.web` и репозиторий в `%TEMP%` по-прежнему дают корпоративную идентичность.
Глобальный конфиг для всего вне этой папки не изменён.

## [2026-08-12] feat | Фаза 5, часть первая: переносимые URL субмодулей и render.yaml

Репозитории городов решено оставить приватными, поэтому сборочному хосту нужен свой
доступ. Подготовлено всё, что не требует токена.

**`.gitmodules` переведён на `https://github.com/Bonzatina/...`.** Алиас
`github-personal` существует только в `~/.ssh/config` этой машины, и на сборочном хосте
`git submodule update --init` по нему не прошёл бы. Локальное удобство SSH сохранено
подстановкой в `~/.gitconfig`:

```
url."git@github-personal:Bonzatina/".insteadOf "https://github.com/Bonzatina/"
```

Проверено без обращения к сети через `git ls-remote --get-url`: все пять https-адресов
разворачиваются в личный SSH-алиас, а посторонний `https://github.com/someoneelse/repo`
не задет — подстановка ограничена префиксом `Bonzatina/`. Указатели субмодулей после
`git submodule sync` не изменились.

**`render.yaml`.** `rootDir: web`, поэтому команда сборки уходит в корень за субмодулями
и возвращается за `npm ci`. Подстановка токена **условная**: при незаданном
`GIT_SUBMODULE_TOKEN` она пропускается, и первый деплой можно попробовать вообще без
токена — Render ходит в GitHub через собственное приложение, и если ему выдан доступ к
пяти репозиториям, субмодули могут выкачаться сами. Если нет — достаточно завести
переменную и передеплоить, файл править не нужно. Shell-часть проверена `bash -n` и
прогоном ветвления с фиктивным токеном и без него.

Отмечена в комментарии классическая ловушка shallow-субмодулей: указатель, отставший от
верхушки ветки, может не найтись в клоне глубиной 1. GitHub разрешает забирать коммит по
SHA, поэтому обычно это разрешается само; если деплой когда-нибудь на этом упадёт —
лечится увеличением глубины.

Не сделано и требует токена: ночной GitHub Action для сдвига указателей. Не сделано и
требует ответа: чем отдавать 421 МБ картинок, если бесплатный план Render упрётся в
лимиты (`wiki` всех пяти городов — 5.3 МБ, ассеты — 421 МБ).

## [2026-08-12] fix | Деплой — Web Service, а не Blueprint

Инструкция была неверной: я предположил Blueprint, потому что у Братиславы лежит
`render.yaml`. Семейство разворачивается вручную созданными **Web Service**, а такой
сервис `render.yaml` игнорирует — значения надо вписывать в дашборд.

`render.yaml` оставлен как записанный источник правды по сборке, старту и окружению, с
явной пометкой, что ручной сервис его не читает. Поля дашборда и однострочная команда
сборки перенесены в `CLAUDE.md`, потому что там их будут искать.

Команда сборки сведена в одну строку (поле дашборда принимает одну шелл-команду):
выход в корень за субмодулями, условная подстановка токена, возврат за `npm ci`.
Проверено `bash -n` и прогоном обеих ветвей с подменёнными `git`/`npm`.

## [2026-08-12] fix | Первый деплой: указатель на незапушенный коммит, затем падение npm

**Попытка 1** упала на `upload-pack: not our ref 1bd2b45` в Братиславе. Причина не в
shallow-клоне: указатель агрегатора ссылался на коммит, которого **нет на GitHub** —
в `wiki_bratislava` лежали два незапушенных локальных коммита (`b5d341b`,
`1bd2b45`), тогда как remote стоял на `b519c9f`. Тот же «Add visitor notes» в
остальных четырёх городах был запушен, Братислава просто выпала. Исправлено
обычным fast-forward пушем города; указатель агрегатора менять не пришлось — он был
записан верно.

Заодно сверены все пять указателей с фактическими верхушками `main` на GitHub:
расхождений больше нет.

Из этого — вывод, который стоит помнить: указатель субмодуля можно записать на коммит,
существующий только локально, и **вся локальная проверка останется зелёной**.
`npm run check` этого не видит, потому что смотрит только на диск. Сверку с remote
надо добавить отдельным флагом.

**Попытка 2**: субмодули прошли все пять без единого `fatal`, упало на
`npm error Exit handler never called!` — внутренний баг npm. `--no-audit --no-fund`,
на который ссылается блюпринт субпроектов, уже стоял. Отличие от городского сервиса:
у нас к моменту запуска npm на диске все пять городов (~430 МБ) вместо одного
(77–192 МБ).

Команда сборки перестроена: `npm ci` идёт **первым**, пока диск занят только
агрегатором, добавлен `--omit=dev` (в рантайме devDependencies не нужны вовсе —
`npm start` это `tsx src/server.ts`, tsx транспилирует своим esbuild, импортов
`typescript`/`@types` в `src` нет), и в хвост добавлены `df -h` и `du -sh wiki_*`.
Диагностика намеренная: если сборка правда упирается в место, пусть лог это скажет,
а не мы будем угадывать.

**Подтверждено:** `GIT_SUBMODULE_TOKEN` не нужен. Берлин приватный и выкачался без
ошибки авторизации — приложению Render доступа хватает. Условная подстановка оставлена
на случай изменений.

## [2026-08-12] fix | «Exit handler never called»: lockfile был привязан к корпоративному прокси

Причина третьего падения деплоя — моя. `npm config get registry` на этой машине —
`http://npmproxy.devexpress.devx:4873/`, и я поставил зависимости через него. В итоге
**все 128 `resolved`-адресов в `package-lock.json` указывали на внутренний хост**,
которого в интернете нет. `npm ci` на Render честно пытался туда дотянуться и умирал с
`Exit handler never called!` — непрозрачное сообщение, за которым стоит недоступный
реестр. Для сравнения: в рабочем lockfile Дрездена все 128 адресов на
`registry.npmjs.org`.

Диагноз стал очевиден только после перестановки `npm ci` вперёд: npm упал **первым**, на
почти пустом диске, чем сама себя опровергла версия про нехватку места из-за 427 МБ
картинок. Перестановка оказалась холостой по эффекту, но полезной по информации.

Что сделано:

- `package-lock.json` перегенерирован против публичного реестра. С первого раза не
  вышло — npm брал адреса из скрытого lockfile внутри `node_modules`, пришлось убрать
  дерево в сторону. Итог: 128 из 128 на `registry.npmjs.org`, вхождений `devexpress` — 0.
- Версии при перегенерации подтянулись внутри диапазонов `^`: 32 пакета, из них значимые
  `tsx 4.23.0 → 4.23.12` и `esbuild 0.28.1 → 0.28.2`. У остальных 96 пакетов
  `integrity` совпал побайтово со старым файлом — прокси отдавал те же тарболы,
  подмены нет.
- `web/.npmrc` фиксирует `registry=https://registry.npmjs.org/`, чтобы привязка к прокси
  не могла вернуться при следующей установке.
- Проверено: `npm ci` с нуля ставит 102 пакета за 10 с, `npm ci --omit=dev --dry-run`
  разрешается, `npm run typecheck` чист на поднятых версиях, `npm run check` — те же
  935 страниц по пяти городам.

Команду сборки в дашборде менять не нужно: она уже правильная, дело было в lockfile.

## [2026-08-12] feat | Rural travel в объединённом сайте: вторая таксономия и единая палитра

Rural — не шестой город, а вторая форма географии. Поэтому в движке появилось понятие
таксономии, а не набор `if`.

**`src/taxonomy.ts`.** Две записи: `city` (район/квартал, транспорт вне охвата) и `rural`
(регион/подрегион, плюс железные дороги и паромы линиями). Загрузчик приводит оба
диалекта frontmatter к **одной внутренней паре `area`/`subarea`**, и ниже по стеку
`district`/`quarter`/`region`/`subregion` не встречаются нигде — ни в шаблонах, ни в
`map.js`. Проверено grep'ом: городских литералов в клиентском скрипте не осталось.

`ignoreDirs` в таксономии — не перестраховка: по умолчанию любая неперечисленная папка
верхнего уровня трактуется **как область** и обыскивается на `typeDirs`. Для rural это
случайно работало (у `sources/` и `raw/` нет таких подпапок), но полагаться на случай
нельзя, поэтому они названы явно. `sources/` — это 400 страниц-источников, которые
собственный движок rural загружает и не рендерит ни в одной секции; мы их просто не
читаем. `raw/` — 20 страниц `type: article`, невидимых и в самом rural.

**Маркеры: три дефекта, которые вылезли при сведении палитр.**

- `transport` у rural был `#9a7bb0`, а `museums` у городов `#8e6c9e` — на маркере в 6
  пикселей это один и тот же фиолетовый. Переключаясь между сайтами, читатель видел
  одинаковый цвет, означающий разное. Транспорт переведён на коричневый `#795548`,
  «плитки» списка тоже (были фиолетовые, что усугубляло).
- `settlement` у rural был `#1565c0` — ровно тот синий, которым `map.js` рисует маркер
  «я здесь», круг точности геолокации и рамку выделения зоны. Переведён на серо-синий
  `#607d8b` городских кварталов: одна роль — один цвет.
- **Музеи и купальни на карте rural не выделялись вообще.** В его `DOMAIN_COLOR` не было
  ни `museums`, ни `thermal`, поэтому 122 музея и 10 купален рисовались обычной оливой,
  тогда как на городских картах они фиолетовые и оранжевые. Контент при этом трогать не
  пришлось: домены во frontmatter уже стояли, `resolveDomainColor` умеет разбирать
  многозначное поле (`domain: museums, architecture`), и во всех случаях `museums` идёт
  первым. Правка — одна строка в цветовой таблице.

Уточнение к формулировке задачи: нужен был не новый **тип**, а новый **домен** в палитре.

Дополнительно `page-map.ts` сужает `domainColors` до заявленных сайтом доменов — иначе
страница с незаявленным доменом получила бы его цвет, но фильтровалась как обычная
достопримечательность. Сегодня таких страниц нет; закрыто конструктивно.

**Презентация.** `kind: 'city' | 'rural'` отделён от таксономии и влияет только на
подачу: лендинг и переключатель группируют по нему, чтобы rural не подавался шестым
городом. Заголовки «Города» и «Сельская местность» появляются только когда есть что
разделять.

Название не меняли — слаг `/rural` относится только к объединённому сайту, папка и
репозиторий сохраняют своё имя. Русская подпись в переключателе оставлена как
`Rural Travel`: выдумывать её значило бы предрешить открытый вопрос о переименовании.

**Результат:** 1815 страниц по шести сайтам (было 948), rural даёт 867 из 1298 файлов —
разница ровно в `sources/` (407), `raw/` (20) и четырёх файлах в корне `wiki/`. Прогон
31 маршрута: все как ожидалось. Легенды: `transport` только у rural, `thermal` у
Будапешта и rural, у остальных без изменений.

**Найдено и НЕ исправлено** (это контент субпроекта, из корня в него не пишем):
дубль слага `Hortobágy` — страница региона `wiki/regions/Hortobágy.md` и страница
населённого пункта `wiki/hortobágy/settlements/Hortobágy.md`. По `/rural/Hortobágy`
достижима только первая; вторая недостижима и в собственном приложении rural, то есть
это не привнесённая нами поломка. Лечится переименованием одного из файлов в субпроекте.

Кросс-сайтовых коллизий слагов стало 5: к `hauptbahnhof` и `kunstgewerbemuseum`
(Берлин/Дрезден) добавились `hrad-devin` (Братислава/rural), `lechner-odon` и
`ybl-miklos` (Будапешт/rural). Все разрешаются корректно — проверено, что
`/bratislava/Hrad-Devin` и `/rural/Hrad-Devin` отдают разные страницы.

Замечено на будущее: картинки rural тяжёлые — в среднем 995 КБ против 399 КБ у Берлина,
39 файлов тяжелее 2 МБ, максимум 19.8 МБ. Деплой вырастает до ~818 МБ картинок.

## [2026-08-12] feat | Вес картинок: 826 МБ → 192 МБ

Замер показал, где лежал вес: CSS показывает картинки шириной не более 440 px
(гербы 220), а файлы коммитились в том разрешении, в каком их отдал источник.
**948 файлов из 1289 были шире удвоенного предела показа, и на них приходилось
790 МБ из 826** — то есть 96% байтов составляли пиксели, которых никто никогда не
видел. Самый тяжёлый файл — 19.8 МБ, чтобы показать картинку в 440 px.

`tools/shrink-images.mjs` — инструмент, а не разовый скрипт, потому что иначе
следующий ингест всё вернёт. Уменьшает JPEG шире 880 px и заново кодирует с
запасом качества. Защиты: оригинал остаётся, если пережатие экономит меньше 10%;
результат отвергается, если это не валидный JPEG нужной ширины; файлы с именем
`.png` не трогаются вовсе (гербы и плоская графика, где JPEG выглядел бы хуже).
Идемпотентен — повторный прогон не пережимает то же дважды. Из зависимостей только
ffmpeg (ImageMagick в системе нет: `convert.exe` — виндовая утилита разметки диска).

| Субпроект | Было | Стало | Пережато |
|---|---|---|---|
| berlin | 191.0 МБ | 74.9 МБ | 286 |
| bratislava | 123.3 МБ | 27.4 МБ | 148 |
| budapest | 30.6 МБ | 10.6 МБ | 48 |
| dresden | 73.7 МБ | 18.3 МБ | 95 |
| wroclav | 5.2 МБ | 2.3 МБ | 14 |
| rural | 395.1 МБ | 51.4 МБ | 292 |
| **всего** | **826 МБ** | **192 МБ** | **883** |

Самый тяжёлый файл семейства теперь 707 КБ вместо 19.8 МБ.

Попутно нашлось: **четыре файла с расширением `.jpg` были на самом деле PNG**.
Браузер их отрисовывал (он смотрит на содержимое), поэтому никто не замечал, но
фотография 1200 px в PNG стоила 2.2 МБ. Три переконвертированы в настоящий JPEG —
расширение стало правдой. Четвёртый, `tegeler-fliess.jpg` в Берлине, остался PNG:
это палитровый PNG на 115 КБ, в JPEG он стал бы **больше**, и защита по 10% верно
оставила оригинал. Проверено, что `ocsa-reformatus-templom.jpg` и `ocsa-tajhaz.jpg`
не дубли — размер совпал случайно, MD5 разные.

Что уменьшилось и что нет: рабочая копия и деплой — сразу, потому что субмодули
тянутся `--depth 1` и качается только верхушка. Репозитории на GitHub **не**
уменьшились: исходные блобы остались в истории, а переписывать шесть историй ради
этого не стоит.

Проверено после прогона: все 1289 файлов декодируются, битых нет, имена и счёт не
изменились (ни одна ссылка со страниц не сломалась), временных файлов не осталось,
страницы рендерятся с картинками, `npm run check` — те же 1815 страниц.

Правило добавлено в `CLAUDE.md` всех шести субпроектов: после ингеста прогонять
инструмент. Без этого объём вернулся бы через несколько ингестов.

Заодно: в коммит rural случайно попал ваш неотслеживаемый `check_coords.js`
(`git add -A` его подобрал). Коммит переписан, файл снова вне репозитория.

## [2026-08-13] feat | Ползунок туристичности — построен, засеян, выключен

Идея: шкала от «все объекты» до «только то, куда не возят автобусами». Реализовано,
данные проставлены для Дрездена и Братиславы, **фича выключена флагом** до вашего
решения.

**Рейтинг из Wikidata, а не из поисковой выдачи.** Позиции в поиске отпали сразу:
API нет ни у Google, ни у Bing (свернули), скрейпинг нарушает условия и ломается,
«примерно N результатов» — нестабильная оценка, а язык запроса перекашивает ось для
русскоязычной вики о Центральной Европе. Число языковых версий Википедии стабильно
годами, бесплатно и берётся одним SPARQL-запросом на город.

**Что пришлось исправлять по ходу — четыре ошибки, каждая найдена измерением.**

1. *Районы съедали объекты внутри себя.* `Amfiteater-Devin` слипся с районом Девин
   (34 ссылки) и получил максимум. Чёрный список теперь строится из собственных
   страниц сайта, а не вписывается руками.
2. *Официальное имя ≠ обиходное.* «Голубой костёл» в Wikidata — `Kostol svätej
   Alžbety`. Добавлены псевдонимы.
3. *Процентильные корзины неверно читали данные.* Ссылки — длинный хвост
   (`47 36 34 31 27 25 | 23 20 20 19…`), и равные пятые части клали в верхнюю
   корзину 24 объекта при пороге 10 ссылок. Нарезка переведена на **долю от
   максимума города**: верхняя корзина стала 7 в Братиславе и 8 в Дрездене — столько
   там и есть сколько-нибудь массовых достопримечательностей. Свойство «в пределах
   города» сохранено.
4. *Заголовки русские, а сравнивал я только с локальными метками.* Русская половина
   заголовка не участвовала вовсе. Добавлены метки и псевдонимы на трёх языках,
   кириллица перестала выбрасываться при нормализации. Дрезден покрыт целиком.

**Правило для ненайденного менялось дважды, и оба раза по делу.** Сначала такие
страницы получали `fame: 1`; когда выяснилось, что сопоставитель промахивается мимо
53 объектов Братиславы включая Михальскую башню, я перестал их оценивать — нельзя
выдавать «не знаем» за «знаем, что тихо». После трёх проходов сопоставления промахи
стали другими: сказочное дерево, «Выдух», мурал «Мир». Отсутствие статьи в любой
языковой Википедии — это и есть первая корзина, и правило вернулось, но теперь
разделяет случаи: **координаты есть и не нашли → 1** (поиск был), **координат нет →
без рейтинга** (поиск был невозможен). Прежнее правило приводило к тому, что самые
«лоитерские» объекты исчезали с карты именно на тихом конце шкалы.

**Отвергнуто: тип источника.** Хотел использовать `sources:` — туристический портал
против каталога памятников. Померил: в Братиславе портал цитируют 15 мест из 157
(10%), в Дрездене 49 из 78 (63%). Град, Михальская башня, Голубой костёл и мавзолей
Хатама Софера ссылаются на одну и ту же городскую газету — признак их не различает.
Глубже: состав источников говорит о том, как автор изучал страницу, а не о самом
месте.

**Что фича всё ещё делает неправильно** (записано в `CLAUDE.md` для того, кто будет
включать): языковое покрытие меряет «как широко написали», а не «сколько идёт людей» —
мавзолей Хатама Софера получает оценку как автобусная остановка; а ненайденный объект
получает 1 по факту отсутствия статьи, что верно для сказочного дерева и неверно для
Собора святого Мартина.

Итог: Дрезден 78 из 78 оценены, Братислава 156 из 157. Флаг `FEATURES.fameSlider` в
`web/src/constants.ts` — **false**; переменная `FAME_SLIDER=on|off` переопределяет его,
чтобы можно было попробовать на живом сайте без коммита.

Попутно исправлен давний дефект: после возврата кнопкой «назад» восстановленные
фильтры применялись к списку, но не к маркерам — на карте оставались все.

## [2026-08-13] feat | Rural разбит на пять кустов, у лендинга появилась карта

**Разбиение.** Rural подаётся пятью сайтами вместо одного. Повод не эстетический:
охват 377 км с запада на восток, и при прежнем стартовом виде **413 из 772 объектов с
координатами были за кадром** — Балатон (113) и Бургенланд (80) целиком. Одна карта
такую площадь обслужить не может.

Кусты сложились по географии сами, содержимое распределено крайне неровно (Балатон 125
страниц против Абауй-Торны с двумя), поэтому дробить по регионам было нельзя:

| куст | страниц | регионов |
|---|---|---|
| Излучина Дуная | 301 | 8 |
| Малые Карпаты и Бургенланд | 278 | 7 |
| Балатон и Задунавье | 236 | 3 |
| Спиш и Словацкое Рудогорье | 144 | 8 |
| Большая равнина | 124 | 5 |

**Репозиторий не тронут.** Разбиение целиком в реестре: запись получила поле `areas`,
загрузчик фильтрует по нему папки областей и страницы регионов. Один `dir`, один
указатель субмодуля, собственное приложение rural работает как прежде. Концепции и
персоналии общие для всех пяти.

Бургенланд отнесён к Братиславе, а не к Венгрии: Айзенштадт в сорока километрах и был
Кишмартоном того же комитата. Разбиение по нынешним границам противоречило бы посылке
самого субпроекта о культурной непрерывности поверх них.

**Про название пятого куста.** Сначала было «Верхняя Венгрия» — исторически точно и
непригодно: в нынешнем употреблении у термина ирредентистский привкус, и он вдобавок
дезориентирует, называя несуществующее сто лет государство применительно к чужой
территории. Очевидная замена на «Среднюю и Восточную Словакию» оказалась бы просто
неверной: проверка координат показала внутри куста три венгерских объекта, включая
Холлокё из списка ЮНЕСКО. Итог — «Спиш и Словацкое Рудогорье»: имена ландшафтов, ни
одного государства как претензии. «Рудогорье» уточнено словом «словацкое», иначе
спуталось бы с Erzgebirge под Дрезденом, который у нас тоже есть. Причина записана
комментарием в реестре, чтобы никто не «исправил» обратно.

Попутно найдено и обойдено: **паром Тихань–Сантод помечен в вики как `type: place`, а не
`ferry`**. Первая версия фильтра маршрутов из-за этого выбросила его линию с карты
Балатона; фильтр сделан нечувствительным к типу. Само несоответствие — контент
субпроекта, не трогали. И проверка коллизий слагов сломалась от разбиения: пять кустов
делят одни концепции, выдавалось полсотни ложных совпадений. Ключ переведён на
субпроект — снова пять настоящих.

**Карта на лендинге.** Сетка карточек ничего не говорит о том, где эти места. Теперь на
экранах от 980 px слева карта семейства с булавками, справа карточки, наведение
связывает одно с другим.

Раскладывать карточки по истинным координатам нельзя — измерено: на холсте 1200×700
**семь сайтов из десяти попадают в одну четверть**, нижняя левая пуста, и девять пар из
сорока пяти налезают друг на друга (Будапешт и Излучина Дуная — 49 px при карточке в
240). Поэтому на карте только булавки, а подписи в карточках.

**Мобильный.** Идея карты на телефоне отвергнута после замера: лендинг там и без неё
занимал **пять экранов прокрутки** (~2920 px одних карточек на экране 375 px). Leaflet
на узкий экран не грузится вовсе — скрипт проверяет ширину до того, как подставить
`<link>` и `<script>`. Ниже 600 px карточка стала строкой с миниатюрой 76×76: около
полутора экранов вместо пяти. Без JavaScript карта не появляется, карточки остаются
полноценным выбором.

## [2026-08-13] feat | Ночной экшен: указатели сдвигаются сами

`.github/workflows/bump-content.yml` — раз в сутки в 03:17 UTC плюс запуск руками из
вкладки Actions. Переводит каждый указатель на верхушку своей ветки и коммитит только
то, что действительно сдвинулось.

**Субмодули не выкачиваются.** Меняться должен один SHA на город, а
`git submodule update --remote` ради этого скачал бы все рабочие копии — около 200 МБ
фотографий каждую ночь, чтобы записать шесть строк. `git ls-remote` отвечает на тот же
вопрос шестью запросами, а `git update-index --cacheinfo` кладёт gitlink прямо в индекс.
Записывать SHA без локальной проверки безопасно: ls-remote только что назвал его
верхушкой ветки.

Побочная выгода: указатель по построению никогда не отстаёт от ветки, а именно
отставший указатель и не находится при выборке `--depth 1`.

Ветка берётся из `.gitmodules` для каждого субмодуля отдельно — у городов `main`, у rural
`master`. Проверено вживую на всех шести перед коммитом: разрешились все, ветки верные,
ложных срабатываний нет.

Недоступный город валит весь прогон, а не пропускается: наполовину обновлённый набор
хуже необновлённого.

Нужен секрет **`SUBMODULE_TOKEN`** — fine-grained PAT с `Contents: Read` на шесть
субпроектов: они приватные, а штатный `GITHUB_TOKEN` ограничен этим репозиторием. Для
пуша обратно хватает `permissions: contents: write`. Коммиты подписаны
`github-actions[bot]`: их не писал человек, и в истории они не должны читаться как чья-то
работа.

YAML и все три шелл-шага проверены разбором и `bash -n`.

## [2026-08-13] fix | Ночной экшен падал с exit 128

Первый ручной запуск умер на `Process completed with exit code 128` без единой
подробности. Две причины, обе мои.

**Чужой токен перебивал наш.** `actions/checkout` записывает заголовок авторизации в
**локальный** конфиг репозитория, привязанный к github.com, со своим токеном — а он
действителен только для `loiter`. Внутри рабочего дерева этот заголовок побеждает
учётные данные из переписанного URL, поэтому каждый запрос к репозиторию города уходил
не от того лица и отвергался. `ls-remote` перенесён в отдельную папку **вне
репозитория**: там действует только глобальный конфиг, где и живёт подстановка с PAT.

**Диагностика была никакая.** `set -e` убивал шаг на первой же неудаче, не сказав ни
какой город, ни что ответил git. Убран в пользу явной обработки: теперь ошибка называет
путь, URL и сообщение git. Заодно проверяется `rev-parse` — без `set -e` нечитаемый
gitlink оставил бы пустую переменную, и это выглядело бы как сдвиг указателя.

`actions/checkout` поднят с v4 до v5: v4 тянет Node 20, который на раннерах объявлен
устаревшим.

Логика прогнана локально ровно в той форме, в какой она теперь в экшене, включая
`ls-remote` из отдельной папки: все шесть разрешились, сдвигать нечего.

## [2026-08-14] fix | Шапка не помещалась на узких экранах

Скриншот с телефонной ширины: «О проекте» переползло на вторую строку и обрезалось,
переключатель RU/EN уехал за правый край.

Причина не в длине названия как таковой, а в том, что **в `shared.css` не было ни одного
медиазапроса** — шапка везде жила по десктопным отступам и кеглю. Пока в переключателе
стояли «Дрезден» и «Берлин», запаса хватало; кусты rural дали названия втрое длиннее
(«Спиш и Словацкое Рудогорье»), и запас кончился.

Три слоя защиты, именно в этом порядке:

1. **Название сжимается.** В `summary` оно было голым текстом, поэтому обрезать его
   отдельно от стрелки `▾` было нечем: многоточие съедало стрелку раньше, чем букву.
   Текст завёрнут в `<span class="city-switcher-name">`, `summary` стал флексом,
   многоточие висит на спане, стрелка — `flex: none`.
   Ключевое здесь `min-width: 0` на `.header-left` и `.city-switcher`: по умолчанию
   флекс-элемент не сжимается уже своего контента, и никакой `overflow` этого не меняет.
2. **Меньше отступы и кегль** до 620 px — освобождает около 70 px, чего хватает,
   чтобы всё четыре элемента стояли в строку примерно до 400 px.
3. **`flex-wrap: wrap`** на самой шапке — на совсем узком нав уходит второй строкой,
   а не за край.

Ничего не прячется: на телефоне переключатель — основной способ перемещаться по сайту,
и убрать его «ради места» значило бы решить проблему за счёт того, ради чего шапка есть.

Выпадающий список ограничен `max-width: calc(100vw - 28px)` — самая длинная строка в нём
шире телефона.

Ловушка по дороге: правило `header span` (0.85rem, opacity .75) начало попадать в новый
спан — класс перебивает два типовых селектора только по тем свойствам, которые называет,
поэтому название вышло бы блёклым и на размер мельче. В `.city-switcher-name` добавлены
явные `font-size: inherit` и `opacity: 1`.

## [2026-08-14] edit | Со страницы «О проекте» убран абзац про заметки

Убран во всех шести проектах — в агрегаторе и в пяти городских — вместе с ключом `notes`
в `page-about.ts`, в русской и английской версии. В `wiki_rural_travel` его никогда не
было.

Абзац объяснял кнопку тому, кто её уже видит: форма стоит прямо на странице объекта,
подписана «Оставить заметку» и несёт собственную строку про приватность. Правило простое
и стоит запомнить: если функция включена и видна, отдельный текст о том, как она
работает, только занимает место на странице «О проекте».

Строка `privacy` в `constants.ts` («Заметка уходит на почту модератора и не публикуется
автоматически…») **не тронута** — это текст внутри самой формы, там он и уместен, потому
что появляется ровно в тот момент, когда человек собирается что-то отправить.

Проверено: строки `t.notes` в дереве не осталось нигде; `tsc` по `page-about.ts` чист во
всех шести. Три ошибки в `src/wiki.ts` субпроектов (типизация `fs.watch`) — они там были
и раньше, к этой правке отношения не имеют.

## [2026-08-14] feat | Шапка в две строки; длинная строка источников больше не тянет страницу

**Шапка.** Верхняя строка — то, что принадлежит сайту и не меняется в ширине: вордмарк,
«О проекте», RU/EN. Нижняя отдана переключателю региона целиком. Так исчезает сама
причина вчерашней тесноты: самая длинная и самая нужная подпись перестаёт делить 400 px
с тремя постоянными контролами. Переключатель заодно получает вес, которого у него не
было, — это то, ради чего сюда приходят с телефона.

Разметка: `.header-left` больше нет, вместо него две `.header-row`; `header` стал
колонкой. Обрезание названия многоточием оставлено — на своей строке оно почти никогда
не сработает, но стоит ноль и страхует очень узкий экран. Медиазапрос теперь только
поджимает отступы и кегль, разгружать ему больше нечего.

Проверено, что ничто не считало высоту шапки: `#map` и карта лендинга заданы в `60vh`,
`calc(100vh - …)` в стилях нет, так что подросшая на ~30 px шапка просто сдвигает
содержимое вниз.

**Источники.** `.src-domain` имел `flex-shrink: 0`. Флекс-элемент, которому запрещено
сжиматься, раскладывается по ширине содержимого независимо от контейнера — и одна строка
`kompmenetrend.hu · kompok-revek.hu · szentendrekomp.hu` (53 знака против 24 у следующей
по длине во всём реестре) растягивала страницу и вешала горизонтальную прокрутку под
каждым списком источников. `flex-shrink` убран; выравнивание колонок держит `min-width`,
он же не даёт обычным строкам ничего заметить. Добавлен `overflow-wrap: anywhere` на
случай одного домена длиннее колонки — сегодня такого нет, но он в одном ингесте отсюда.

Ниже 620 px две колонки становятся двумя строками: 240 px под домен оставляли описанию
полоску, в которой не читается.

Строка эта существует ровно в двух местах — в реестре агрегатора (`web/src/about/rural.ts`)
и в собственном приложении `wiki_rural_travel`. Второе не тронуто: правка в субпроект
из корня не идёт без отдельной просьбы. У остальных четырёх городов CSS так же хрупок,
но многодоменных строк у них нет, и видимой поломки тоже.

## [2026-08-14] feat | Шестой город: Вена подключена к агрегатору

Новый субпроект `wiki_viena` (репозиторий `Bonzatina/Loiter-viena`) заведён как подмодуль
и зарегистрирован в `web/src/cities.ts`. Контента пока нет — только каркас вики; город
появляется в пикере с нулём страниц и пустой картой, и это ожидаемое состояние до первого
ингеста.

Запись реестра: `slug: 'vienna'`, `dir: 'wiki_viena'`, `kind: 'city'`, `CITY_TAXONOMY`,
`NO_ROUTES`, центр `[48.2082, 16.3738]`, zoom 12, `Europe/Vienna`, `districtLabel: 'Район'`,
домены `museums / nature / lookout`, карточка `stephansdom.jpg`.

Три вещи, которые стоит помнить об этой записи:

- **Слаг ≠ каталог, снова.** Папка создана как `wiki_viena`, и репозиторий носит это же имя,
  а читателю город отдаётся по `/vienna`. Прецедент — Wrocław из `wiki_wroclav`; поле `dir`
  ровно для этого и существует.
- **`districtLabel: 'Район'`, а не берлинское «Округ»**, хотя у Вены тоже Bezirke: по-русски
  о венских районах говорят «район», и это единственное место, где строка видна читателю —
  в письме с заметкой.
- **`thermal` не включён.** У Вены есть кандидат (Therme Wien в Оберлаа), но домен в реестре
  перечисляется только тогда, когда под ним есть объекты, иначе в легенде появится пустая
  кнопка.

Карточка пикера берётся из ассетов самого города (`assetsPrefix` → `/{slug}/assets`), а не
из `web/assets` агрегатора, поэтому `stephansdom.jpg` лежит в `wiki_viena/web/assets/`
и прогнан через `tools/shrink-images.mjs` (0.8 МБ → 0.3 МБ).

Побочно исправлено: **пустой список источников на странице «О проекте»**. `renderAboutPage`
печатал заголовок «Источники», вводную фразу, абзац о правах на изображения и пустой `<ul>`
— для города, у которого источников ещё нет. Теперь при `sources.length === 0` выводится
одна строка «Город только начат…», а список и абзац об изображениях не рендерятся вовсе:
абзац о правах осмыслен только рядом с перечнем. У пяти остальных городов вывод не изменился
(проверено: у Братиславы по-прежнему 48 строк).

Проверено на живом сервере: `/`, `/vienna`, `/vienna/about`, `/en/vienna`, `/en/vienna/about`
и `/vienna/assets/stephansdom.jpg` отдают 200; кэш вики прогревается строкой `vienna 0`
без ошибок — пустой город не ломает ни загрузчик, ни карту, ни счётчик страниц («0 страниц»
через `plural()`).

## [2026-08-14] edit | Источники Вены на странице «О проекте»

`web/src/about/vienna.ts` получил первые три строки — `geschichtewiki.wien.gv.at`,
`de.wikipedia.org`, `commons.wikimedia.org`. До этого список был пуст, и страница
`/vienna/about` показывала заглушку «город только начат»; теперь она рендерит обычный
перечень источников с абзацем о правах на изображения. Повод — первый содержательный
ингест в субпроекте (23 района, 46 страниц), он записан в `wiki_viena/wiki/log.md`.

Движок не менялся. `npm run check`: 2054 страницы по 11 сайтам, у Вены
`23 pages / 23 mapped / 23 en`.

## [2026-08-15] feat | Вена: 31 новая страница, карта вышла за пределы первого района

Пять ингестов в `wiki_viena` (Дёблинг, Кольцо, Отто Вагнер, «красная Вена», Пратер).
Для агрегатора это только контент — ни одной правки в `web/src`, `web/scripts` или
`web/styles` не потребовалось, что и есть проверка схемы: новые районы, кварталы и
объекты подхватываются загрузчиком по соглашению о папках.

`npm run check`: Вена — **73 страницы, 63 с координатами, 73 EN**; place 37, district 23,
concept 5, person 5, quarter 3. Первые три страницы `type: quarter` в этом городе, то есть
первые сине-серые маркеры на венской карте. Новых доменов-источников не появилось —
`src/about/vienna.ts` не трогался.

Список коллизий слагов вырос на одну: `staatsoper` теперь в Берлине и в Вене. Городское
пространство имён с этим справляется, менять ничего не нужно; запись оставлена как
подтверждение, что правило `/{city}/{slug}` продолжает окупаться.

Единственная проблема в выводе проверки — дубликат `Hortobágy` в сельской вики,
существовавший до этой работы и к Вене отношения не имеющий.

## [2026-08-15] edit | Вена: ещё 21 страница, пять новых доменов-источников

Три ингеста в `wiki_viena` за день — Хитцинг (Шёнбрунн, Глориетта, зоопарк, Лайнцский зверинец,
Хермесвилла, кладбище), юго-восточная дуга (Бельведер, Хундертвассерхаус, Арсенал, Центральное
кладбище, газометры, Нойгебойде) и «названные, но не написанные» (Карлскирхе, Нашмаркт, Музейный
квартал, Шпительберг, Фрейд, Сервитенфиртель), плюс три персоналии.

`npm run check`: Вена — **94 страницы, 81 с координатами, 94 EN**; place 53, district 23,
person 8, concept 5, quarter 5. Объекты теперь в семнадцати районах из двадцати трёх.

Единственная правка в агрегаторе — `web/src/about/vienna.ts`: пять новых доменов
(`wien.gv.at`, `schoenbrunn.at`, `zoovienna.at`, `wienmuseum.at`, `friedhoefewien.at`,
`belvedere.at`, `freud-museum.at`). Движок снова не менялся: районы 03, 04, 06, 07, 11 и 13
получили первые объекты и подхватились загрузчиком по соглашению о папках.

Побочно: в `wiki_viena` исправлены одиннадцать неверных координат — проверялись обратным
геокодированием с сверкой почтового индекса против объявленного района. У маркера Веринга индекс
оказался хернальсский. Той же проверкой стоит когда-нибудь пройтись по остальным городам.

Новая межгородская коллизия слагов: `staatsoper` — Берлин и Вена. Пространство имён города её
закрывает, но в списке `npm run check` она теперь шестая.

## [2026-08-16] feat | Два домена источников в about Братиславы

`web/src/about/bratislava.ts` получил `staremesto.sk` и `asb.sk` — оба пришли с двумя
новыми страницами дворцов на Штефаниковой (Карачони и Пистори), написанными в
`wiki_bratislava`. Первый встал к официальным сайтам, сразу за `bratislava.sk`, второй —
к архитектурным изданиям, за `yimba.sk`; порядок в списке тематический, и оба заняли
место по теме, а не в конец.

Движок не тронут: обе страницы — обычные `place` в существующей папке `1-stare-mesto`,
загрузчик берёт их по соглашению. Указатель `wiki_bratislava` переставлен здесь же на
свежий коммит субпроекта — ночной `bump-content.yml` сделал бы то же самое к утру, но
страницы и домены выходят одной правкой и появляются на объединённом сайте сразу.

## [2026-08-19] feat | Скил вычитки: `.claude/skills/proofread/`

Из пятидневной вычитки венской вики сделан скил, потому что метод переносится, а половина
находок — нет.

**Что в нём.** Порядок работы (сортировать не по дате, а по «объём × скорость написания»: 23
районных обзора по 430 слов, написанные за день, дали 25 дефектов из 47, а объекты того же дня —
ноль), три скрипта и копилка дефектов автора, которую надо пополнять после каждого захода.

**Скрипт, ради которого всё это стоило собирать, — `consistency.py`.** Он показывает имена, которые
корпус пишет двумя способами. Для имён собственных словарь бесполезен, и единственный надёжный
признак опечатки — что текст противоречит сам себе. На вычитанной вручную вики он немедленно нашёл
две ошибки, которых чтение не заметило («Марияхильферштрассе», «Вильхельма Елинека»); всего таких
ошибок было четыре, и глазами нашлись две, обе случайно. Он знает, что падежные окончания — не
различие, и что `ae/oe/ue/ss` против умляутов — это соглашение об именах файлов, поэтому на чужом
корпусе молчит: на Дрездене, который писал не я, шесть находок, все законные.

**Честные числа вынесены в сам скил.** Чтение нашло 44 дефекта из 49, `consistency.py` — 3,
детектор устойчивых дефектов — 1, детектор повторов — 1. Скрипты не заменяют чтения, и в SKILL.md
это написано прямо, чтобы «проверки прошли чисто» не выдавалось за вычитку.

**И то, за что заплачено дороже всего:** три дефекта из сорока девяти внесены предыдущими
заходами вычитки. Правка одной строки без перечитывания абзаца создаёт ошибки примерно так же
легко, как убирает.

Скил лежит в репозитории семейства, а не в `~/.claude/skills/`: он знает правила этих вики —
парность RU/EN, запрет на упоминание источников в тексте, проверку координат обратным
геокодированием по `city_district`, а не по индексу.

## [2026-08-19] lint | Скил вычитки прогнан по всем шести субпроектам

Запущен `consistency.py` на Берлине, Братиславе, Будапеште, Дрездене и Вроцлаве. Детектор,
написанный под одну вики, немедленно оказался переобучен под неё — и два правила, которых
не хватало, важнее всего остального в первой версии.

**Заглавная буква после точки — не имя.** «Построенная» против «Постоянной», «Поселение»
против «Посещения»: обычные слова, оказавшиеся в начале предложения, давали большую часть
шума. Теперь сравниваются только слова с заглавной буквы **внутри** предложения. Берлин: 37 → 25.

**Диакритика — соглашение о слагах во всех языках семейства, а не только в немецком.**
Jurkovičova/Jurkovicova, Országház/Orszaghaz, Erzsébet/Erzsebet — в одной Братиславе таких пар
было одиннадцать, и они хоронили под собой всё настоящее. Подавлены по умолчанию, показываются
по `--diacritics`, потому что проза действительно может терять надстрочные знаки, и это уже дефект.
Братислава: 31 → 13.

**Что нашлось (в чужих субпроектах не правил — по правилу корневого CLAUDE.md):**

- **Берлин.** Страница `Lindenufer` дважды называет шпандаускую Николаикирхе «Николайкирхе»,
  тогда как собственная страница церкви и остальной Шпандау пишут «Николаикирхе»; митовская
  церковь при этом законно «Николайкирхе» — это два разных храма с разными немецкими именами.
  Ещё: «Мессдамм-Унтерфюрунг» против «Перехода под Месседамм» в заголовке самой страницы,
  «Херманплац» против «Херманнплац», «Бланкенштайна» против «Бланкенштейна».
- **Братислава.** «Пресбург» дважды против «Прессбурга» сорок девять раз; «Легионарской»
  против «Легионерской».
- **Будапешт.** «Земмельвайсу» против «Земмельвейса» на соседних страницах.
- **Дрезден.** Ноль: все четыре находки законны (Barricade/Barrikade — английский против
  немецкого, Нойштедтер/Нойштадт — прилагательное против существительного).
- **Вроцлав.** «Грабишинский» против «Грабишиньской».

Итого примерно одна находка из пяти настоящая. Это стоит минуты разбора и не стоило бы часа —
проверку надо держать дешёвой и ничего ею не блокировать. Числа занесены в копилку скила.
